-server和-client指定jvm运行时时server模式还是client模式：

        server VM启动时较慢，但是随着程序的运行会越来越快。


原因如下：

    .java文件编译之后成为.class文件，也就是字节码文件。jvm就是执行字节码的，然后对于特定的
    机器平台来说，要把字节码转换成机器码才可以运行。jvm一边解释java字节码一边执行java程序。
    对于有的java字节码，java会通过jit(即时编译器)将java字节码翻译成特定的机器码，这样下次
    再执行相应的字节码就不用解释并执行了。


    对于-server（server VM）来说，就是使用了jit(即时编译器)技术，将大频率使用的字节码翻译
    成相应平台的机器码。从而节省解释时间。


